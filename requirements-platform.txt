# Platform-specific requirements for JARVIS Computer Assistant

# =============================================================================
# CORE REQUIREMENTS (Cross-Platform)
# =============================================================================
# These packages work on both Windows and Linux
python-dotenv>=1.0.0
PyQt6>=6.4.0
httpx>=0.24.0
requests>=2.31.0
psutil>=5.8.0
schedule>=1.1.0
cryptography>=41.0.0
pathlib>=1.0.1
SpeechRecognition>=3.10.0
colorama>=0.4.6
pyautogui>=0.9.54
sounddevice>=0.4.6
numpy>=1.21.0
opencv-python>=4.8.0
pytesseract>=0.3.10
Pillow>=10.0.0
jsonschema>=4.17.3
pycryptodome>=3.19.0
gTTS>=2.5.0
edge-tts>=6.1.9
simpleaudio>=1.0.4
playsound>=1.3.0
soundfile>=0.12.1
fastapi>=0.104.0
uvicorn>=0.24.0
websockets>=12.0
pydantic>=2.5.0
sqlalchemy>=2.0.0
alembic>=1.13.0
redis>=5.0.0
chromadb>=0.4.0
sentence-transformers>=2.2.0
langchain>=0.1.0
openai>=1.0.0
google-generativeai>=0.3.0
anthropic>=0.7.0

# =============================================================================
# WINDOWS-SPECIFIC REQUIREMENTS
# =============================================================================
# These packages only work on Windows
pywin32>=305; sys_platform == "win32"
comtypes>=1.1.10; sys_platform == "win32"
pycaw>=20181226; sys_platform == "win32"
screen-brightness-control>=0.9.0; sys_platform == "win32"
pyttsx3>=2.90; sys_platform == "win32"

# =============================================================================
# LINUX-SPECIFIC REQUIREMENTS
# =============================================================================
# These packages only work on Linux
# Note: Some Linux packages need to be installed via system package manager
# See config/platforms/linux.json for system dependencies

# =============================================================================
# OPTIONAL PLATFORM-SPECIFIC REQUIREMENTS
# =============================================================================
# These packages provide additional functionality but are not required

# Windows optional
keyboard>=0.13.5; sys_platform == "win32"
pygetwindow>=0.0.9; sys_platform == "win32"
screeninfo>=0.8; sys_platform == "win32"

# Linux optional
# Most Linux audio/video packages are installed via system package manager
# but we can include Python bindings if available

# =============================================================================
# DEVELOPMENT REQUIREMENTS (Cross-Platform)
# =============================================================================
# These are included in requirements-dev.txt but listed here for reference
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
black>=23.12.1
isort>=5.13.2
flake8>=7.0.0
pylint>=3.0.0
mypy>=1.8.0
bandit>=1.7.5
pre-commit>=3.5.0

# =============================================================================
# PLATFORM DETECTION AND CONDITIONAL IMPORTS
# =============================================================================
# Use these patterns in your code:

# For platform detection:
# import platform
# if platform.system() == "Windows":
#     import win32api
# elif platform.system() == "Linux":
#     import subprocess

# For conditional requirements:
# try:
#     import win32api
#     WINDOWS_AVAILABLE = True
# except ImportError:
#     WINDOWS_AVAILABLE = False

# For graceful degradation:
# if WINDOWS_AVAILABLE:
#     # Use Windows-specific functionality
# else:
#     # Use cross-platform alternative
